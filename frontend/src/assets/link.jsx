
const Pink = ({code,link}) => {
  return (
    <div className=''>
        <a href={link} target='blank'><button className="btn bg-yellow-100">{code}</button></a>
    </div>
  )
}

export default Pink;
